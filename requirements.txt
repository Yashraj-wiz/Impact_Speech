# ── Core audio ────────────────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.0
pyloudnorm>=0.1.1
scaper>=1.7.0
numpy>=1.24.0
scipy>=1.11.0

# ── Deep learning / models ────────────────────────────────────────────────────
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.45.0
accelerate>=0.27.0
bitsandbytes>=0.43.0   # quantization; Windows users: install wheel from bitsandbytes-windows

# ── TTS (plan §2.3) ───────────────────────────────────────────────────────────
kokoro>=0.9.2

# ── ASR for manipulation check + GLM-4-Voice transcription (plan §5.2) ───────
openai-whisper>=20231117

# ── Audio tagging for manipulation check (plan §4) ───────────────────────────
# Uses MIT/ast-finetuned-audioset-10-10-0.4593 from HuggingFace
# (no extra pip package needed beyond transformers)

# ── Statistics / data ─────────────────────────────────────────────────────────
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
jiwer>=3.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
jsonlines>=3.1.0
requests>=2.31.0
